# T-043 independent evaluation

Reviewed: 2026-08-25

## Product-design verdict

**PASS.** The independent critic certified the running experience after the
implementation corrected identity consistency, evidence disclosure, approval
consequences, responsive control occlusion, placeholder actions, and visual
direction differentiation.

The final evidence shows complete portfolio, product, and agentic-product
identities; a real demonstration gate with partial, loading, ready, approved,
rejected, cancelled, and recovery behavior; a non-obscuring desktop direction
lab; and a collapsed, focus-managed mobile control. All 18 macOS visual cases
passed for three archetypes, three directions, and two viewports.

## Adversarial QA verdict

**PASS.** The independent evaluator found and verified fixes for incomplete
profile inheritance, invalid machine-readable output, creation-only validation
leaking into normal project use, archetype-specific test leakage, stale
direction output, incomplete dependency detection, reduced-motion coverage,
and no-argument guided-flow routing.

The focused 38-test generator/router/design suite passes. A freshly generated
product project installs from the reviewed lockfile without downloads, builds,
passes its selected-archetype browser suite, and passes complete downstream
verification.

## Security and authority verdict

**PASS.** A separate read-only reviewer first identified two verification gaps,
then confirmed their fixes at exact code head `b6e364f`. Ongoing generated
verification now resolves the profiles currently selected by the project,
validates the surfaces those profiles require, rejects unknown or
profile-incompatible specialists, and accepts MCP only when fully disabled or
an exact match for the reviewed compatibility policy.

The focused authority/profile suite passes 45 tests. Regression coverage proves
that an unknown specialist and an unreviewed `sh -c` MCP entry fail through the
public generated `./agentic verify full` path. Profile transitions prove that
newly required surfaces and inactive surfaces are evaluated from current state,
not immutable creation provenance. Validation performs no server execution,
credential access, installation, or authority expansion.

## Human authority retained

Linux visual candidates remain subject to the existing human baseline-review
workflow. These evaluator verdicts do not approve the task or pull request,
accept a visual baseline, deploy, release, or merge.
