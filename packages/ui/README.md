# UI foundations

This package holds accessible behavior contracts and deliberately minimal
primitives. It does not impose a visual identity. Product surfaces style these
structural donors through their approved semantic tokens and direction.

Add a component here only when its behavior and API are shared. Do not promote a
one-off composition into the system merely because it looks reusable.
